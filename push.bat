@echo off
setlocal enabledelayedexpansion

git config --global user.email "saadatif45@gmail.com"
git config --global user.name "saad0909"

git add .
git commit -m "new user"
git push origin RazaMehdi-patch-1

pause